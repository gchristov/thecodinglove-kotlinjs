server {  
  listen 80;
  server_name landing-page-service;
  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }

  location /${APP_SERVICE_ROOT} {
    proxy_pass ${APP_SERVICE_HOST}/${APP_SERVICE_ROOT};
    proxy_ssl_server_name on;
  }
}